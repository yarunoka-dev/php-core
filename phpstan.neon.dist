parameters:
    level: max
    paths:
        - src/
        - tests/
        - tools/
    # PHPUnit and php-parser live in their own vendor-bin namespaces, outside the root autoloader
    bootstrapFiles:
        - vendor-bin/phpunit/vendor/autoload.php
        - vendor-bin/docgen/vendor/autoload.php
