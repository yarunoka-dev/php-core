<?php

namespace Yarunoka\Expression;

/**
 * An all-day occurrence ({"allday": true}) — the explicit statement of a
 * day-level occurrence that carries no time.
 */
final readonly class AllDay implements TimesSpec {}
